import styled from "styled-components";
import { COLORS } from "../../styles/variablesCss";

const StyledRange = styled.input`
    width: 72%;
    height: 3px;
    accent-color: ${COLORS.rangeButton};
`

export {StyledRange}